apiVersion: v1
kind: Service
metadata:
  name: {{ include "topic-browser.fullname" . }}
  labels:
    {{- include "topic-browser.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "topic-browser.selectorLabels" . | nindent 4 }}
